/* Maida Bulker FMS — Supabase connection
 * Replace only the two placeholder values below.
 * Use the public ANON key. Never put the service-role key in this file.
 */
window.SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_KEY = "YOUR-ANON-KEY";

/* Optional: URL of your Google Apps Script mirror web app.
 * Leave blank when the Google Sheet uses its own 5-minute pull trigger.
 */
window.GOOGLE_MIRROR_URL = "";

